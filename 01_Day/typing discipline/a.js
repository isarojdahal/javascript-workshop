function adder(a, b) {
  return a + b;
}

let sum = 0;
sum = adder(1, 2);

// JavaScript is a dynamically typed language
// It means we do not need to mention the data type in advance.
// The code translator in this case the interpreter will figure out the
// appropriate data type during parsing.

// JIT - Just-in-Time Compilation
// In JIT compilation the compilation is done during execution of a program
// rather than before execution.
