dynamic adder(int a, b) {
  return a + b;
}

void main() {
  int sum = 0;
  sum = adder(1, 2);
}

// Dart is a gradually typed language
// It means we can mention the data type in advance or leave it upto the
// compiler to figure out the data type during parsing.

// It is recommended to mention appropriate data type in advance if we know which
// data type it is gonna take later.
